# Do not log query strings: the OAuth callback query contains a short-lived code and state.
log_format admin_sanitized '$remote_addr [$time_local] '
                           '"$request_method $uri $server_protocol" '
                           '$status $body_bytes_sent "$http_user_agent"';
